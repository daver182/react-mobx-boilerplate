import React from 'react';
import { Component } from 'react';
import style from './style';

export default class App extends Component {
	render() {
		return (
			<button id={ style.button }>Cargar</button>
		);
	}
}